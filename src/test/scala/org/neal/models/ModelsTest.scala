package org.neal.models

import com.typesafe.scalalogging.LazyLogging
import org.scalatest.flatspec.AnyFlatSpecLike
import org.scalatest.matchers.must.Matchers

class ModelsTest extends AnyFlatSpecLike with Matchers with LazyLogging {

  // it should encode and decode a forecast object

  // it should encode and decode a grid coordinates object
}
